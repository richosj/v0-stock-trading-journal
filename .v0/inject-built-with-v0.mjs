// This project no longer uses the original v0 build injection step,
// but Vercel is still configured to execute this script before `next build`.
// Keep it as a harmless no-op so existing build settings continue to work.

console.log("[v0] inject-built-with-v0: skipped");
